<think>
Here's a thinking process that leads to the suggested website code:

1.  **Analyze the Request:**

    *   **Business Name:** Walled Burger and Pizza (implying a brand focused on burgers and pizza, potentially with a "walled" aesthetic, perhaps referring to old walled city of Lahore or just a strong enclosure theme).
    *   **Location:** Fateh Garh Tajpura, Lahore, Pakistan.
    *   **Contact:** +92 303 7869752.
    *   **Map Coordinates:** HC84+Q8 Lahore, Pakistan.
    *   **Hours:** Open 24 hours (closes 3 AM), with a note about Eid al-Adha.
    *   **Features Needed:**
        *   Great graphics (visual appeal).
        *   Online ordering system (cart functionality).
        *   Detailed order display for the owner (admin dashboard view).
    *   **Technical Requirement:** "Use best programe" (implies using standard web technologies: HTML, CSS, JavaScript for a frontend demo).

2.  **Determine the Architecture:**

    *   Since I cannot host a full backend (database + server + payment processing) in a single code snippet response, I must build a *functional frontend prototype*.
    *   *Technologies:* HTML5, CSS3 (Custom variables, Flexbox/Grid for layout, responsive design), JavaScript (Vanilla ES6 for logic).
    *   *Design Theme:* Since the name is "Walled Burger", I'll use a color palette reminiscent of Lahore's heritage (Terracotta, Gold, Deep Red) or a modern fast-food aesthetic (Red/Yellow/Black). Let's go with a modern, warm, appetizing palette: Burnt Orange, Dark Charcoal, and Cream.

3.  **Drafting the Content & Structure:**

    *   **Header:** Logo, Navigation, Cart Count.
    *   **Hero Section:** Appetizing background image, "Order Now" CTA.
    *   **Menu Section:** Categories (Burgers, Pizza, Drinks). Cards with images, prices, "Add to Cart" buttons.
    *   **Cart Modal/Sidebar:** List of selected items, total price, checkout form (simulated).
    *   **Admin/Owner View:** A special section or button to "View Orders" which shows the details in a table format.

4.  **Writing the Code (Iterative Process):**

    *   **HTML:**
        *   Define semantic tags (`header`, `nav`, `section`, `footer`).
        *   Create the menu items using `data-` attributes to store price and ID for JS logic.
        *   Create the Cart Modal structure.
        *   Create the Admin Dashboard structure (hidden by default).

    *   **CSS (Styling):**
        *   *Variables:* Define colors (`--primary`, `--secondary`).
        *   *Reset:* Basic box-sizing.
        *   *Hero:* Use a placeholder image URL (Unsplash) related to burgers/pizza.
        *   *Cards:* Shadow, rounded corners, hover effects.
        *   *Buttons:* distinctive styles for "Add to Cart".
        *   *Modal:* Fixed position, overlay.

    *   **JavaScript (Functionality):**
        *   *State Management:* Need an array `cart = []` and `orders = []`.
        *   *Add to Cart Function:* specific logic to check if item exists (increment qty) or add new.
        *   *Render Cart:* Loop through cart array and create HTML.
        *   *Calculate Total:* Reduce function.
        *   *Checkout:* Simulate sending order to "Owner View".
        *   *Admin View:* Toggle visibility and render the order list.

5.  **Refining the Specifics (Input Data):**

    *   *Address:* "Fateh Garh Tajpura, Lahore, Pakistan" -> Put in Footer.
    *   *Phone:* "+92 303 7869752" -> Click-to-call link.
    *   *Hours:* Mention "Open" and "Eid" note.

6.  **Adding "Great Graphics" (UX/UI):**

    *   Use Font Awesome for icons (Hamburger, Pizza, Phone, Map).
    *   Use Google Fonts (Poppins or Roboto) for clean typography.
    *   Add smooth scrolling.
    *   Make the UI "Pop" with animations.

7.  **Implementing the "Owner Detail" Request:**

    *   I need to create a specific "Admin Panel" simulation.
    *   I will add a button labeled "Owner Dashboard" (secret or visible) that reveals the orders placed by the customer along with the contact info.

8.  **Code Assembly & Review:**

    *   *Self-Correction during coding:* Initially, I might forget the map link. I'll add a Google Maps link using the coordinates provided.
    *   *Check:* Does it handle the math? Yes. Does it show the details? Yes.

9.  **Final Output Generation:** (This matches the provided good response, combining HTML, CSS, and JS into a single copy-pasteable file structure for ease of use).

    *   *Note:* I will use placeholder images from Unsplash so the user sees actual food immediately.

10. **Instructions:** Add a guide on how to use this code (save as .html) and what next steps to take (backend integration).

**(Self-Correction/Refinement details):** The user asked for the details of the order to show the owner. A frontend only code cannot *actually* email the owner or save to a database permanently. I will simulate this by creating an "Incoming Orders" table in the browser that updates when the user clicks "Place Order", allowing the user to test the flow immediately.

11. **Final Polish:** Ensure the address "Fateh Garh Tajpura" is prominent.

**(Proceed to generate output)**
</think>

Here is a complete, single-page website solution.

This code includes **HTML5**, **CSS3** (for great graphics and animations), and **JavaScript** (functionality).

### Features included:
1.  **Modern Design:** Appetizing color scheme (Orange/Charcoal) matching a burger brand.
2.  **Menu System:** Interactive cards for Burgers, Pizzas, and Deals.
3.  **Cart Logic:** Add items, calculate total, and checkout.
4.  **Owner Dashboard:** A hidden panel where the owner (you) can see the incoming live orders with customer details.
5.  **Contact & Location:** Fully integrated with the details you provided (Fateh Garh, phone, map).

#### How to use:
1.  Copy the code block below.
2.  Create a new file on your computer named `index.html`.
3.  Paste the code and save.
4.  Open the file in any web browser (Chrome, Safari, etc.).

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Walled Burger & Pizza | Lahore</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        /* --- CSS VARIABLES & RESET --- */
        :root {
            --primary: #ff6b35; /* Appetizing Orange */
            --secondary: #004e89; /* Deep Blue */
            --dark: #1a1a1a;
            --light: #f7f7f7;
            --gold: #ffb703;
            --shadow: 0 5px 15px rgba(0,0,0,0.2);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            scroll-behavior: smooth;
        }

        body {
            background-color: var(--light);
            color: var(--dark);
        }

        /* --- HEADER --- */
        header {
            background: var(--dark);
            color: white;
            padding: 1rem 5%;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: sticky;
            top: 0;
            z-index: 1000;
            box-shadow: var(--shadow);
        }

        .logo {
            font-size: 1.8rem;
            font-weight: 800;
            color: var(--primary);
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        .logo span { color: white; }

        nav ul {
            display: flex;
            list-style: none;
            gap: 20px;
        }

        nav a {
            color: white;
            text-decoration: none;
            font-weight: 500;
            transition: 0.3s;
        }

        nav a:hover { color: var(--primary); }

        .cart-icon {
            cursor: pointer;
            position: relative;
        }

        .cart-count {
            position: absolute;
            top: -8px;
            right: -8px;
            background: var(--primary);
            color: white;
            border-radius: 50%;
            padding: 2px 6px;
            font-size: 0.7rem;
            font-weight: bold;
        }

        /* --- HERO SECTION --- */
        .hero {
            height: 80vh;
            background: linear-gradient(rgba(0,0,0,0.6), rgba(0,0,0,0.6)), url('https://images.unsplash.com/photo-1568901346375-23c9450c58cd?ixlib=rb-1.2.1&auto=format&fit=crop&w=1350&q=80');
            background-size: cover;
            background-position: center;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            color: white;
        }

        .hero h1 {
            font-size: 4rem;
            margin-bottom: 20px;
            text-shadow: 2px 2px 4px rgba(0,0,0,0.8);
        }

        .btn {
            padding: 12px 30px;
            background: var(--primary);
            color: white;
            border: none;
            border-radius: 50px;
            font-size: 1.2rem;
            font-weight: bold;
            cursor: pointer;
            transition: 0.3s;
            text-decoration: none;
        }

        .btn:hover {
            background: var(--gold);
            color: var(--dark);
            transform: scale(1.05);
        }

        /* --- MENU SECTION --- */
        .menu-section {
            padding: 4rem 10%;
        }

        .section-title {
            text-align: center;
            margin-bottom: 3rem;
            font-size: 2.5rem;
            color: var(--dark);
        }

        .category-tabs {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-bottom: 30px;
        }

        .tab-btn {
            padding: 10px 20px;
            border: 2px solid var(--primary);
            background: transparent;
            color: var(--primary);
            font-weight: bold;
            border-radius: 5px;
            cursor: pointer;
        }

        .tab-btn.active, .tab-btn:hover {
            background: var(--primary);
            color: white;
        }

        .menu-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 30px;
        }

        .food-card {
            background: white;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: var(--shadow);
            transition: 0.3s;
        }

        .food-card:hover {
            transform: translateY(-10px);
        }

        .food-img {
            width: 100%;
            height: 200px;
            object-fit: cover;
        }

        .card-content {
            padding: 20px;
        }

        .card-content h3 { margin-bottom: 10px; }
        
        .price {
            color: var(--primary);
            font-weight: bold;
            font-size: 1.2rem;
            display: block;
            margin-bottom: 15px;
        }

        .add-btn {
            width: 100%;
            padding: 10px;
            background: var(--secondary);
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-weight: bold;
        }

        .add-btn:hover { background: var(--dark); }

        /* --- CART MODAL --- */
        .cart-modal {
            position: fixed;
            top: 0;
            right: -100%;
            width: 100%;
            max-width: 400px;
            height: 100%;
            background: white;
            z-index: 2000;
            transition: 0.4s;
            display: flex;
            flex-direction: column;
            box-shadow: -5px 0 15px rgba(0,0,0,0.2);
        }

        .cart-modal.active { right: 0; }

        .cart-header {
            padding: 20px;
            background: var(--dark);
            color: white;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .close-cart { cursor: pointer; font-size: 1.5rem; }

        .cart-items {
            flex: 1;
            overflow-y: auto;
            padding: 20px;
        }

        .cart-item {
            display: flex;
            justify-content: space-between;
            align-items: center;
            border-bottom: 1px solid #eee;
            padding-bottom: 15px;
            margin-bottom: 15px;
        }

        .cart-footer {
            padding: 20px;
            border-top: 1px solid #ddd;
        }

        .total-display {
            display: flex;
            justify-content: space-between;
            font-size: 1.5rem;
            font-weight: bold;
            margin-bottom: 20px;
        }

        /* --- INFO SECTION --- */
        .info-section {
            display: flex;
            flex-wrap: wrap;
            padding: 4rem 10%;
            background: var(--dark);
            color: white;
            gap: 40px;
        }

        .info-box { flex: 1; min-width: 300px; }
        
        .info-box h3 { color: var(--primary); margin-bottom: 15px; border-bottom: 2px solid var(--primary); display: inline-block;}
        
        .info-box p { margin-bottom: 10px; line-height: 1.6; }

        /* --- OWNER DASHBOARD (HIDDEN) --- */
        #owner-dashboard {
            display: none;
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 80%;
            height: 80%;
            background: white;
            z-index: 3000;
            box-shadow: 0 0 20px rgba(0,0,0,0.5);
            padding: 20px;
            overflow-y: auto;
            border-radius: 10px;
        }

        .admin-header {
            background: #333;
            color: white;
            padding: 10px;
            display: flex;
            justify-content: space-between;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }

        th, td {
            border: 1px solid #ddd;
            padding: 10px;
            text-align: left;
        }

        th { background: #f2f2f2; }

        /* Responsive */
        @media (max-width: 768px) {
            .hero h1 { font-size: 2.5rem; }
            nav { display: none; } /* Simple hiding for mobile demo */
        }
    </style>
</head>
<body>

    <!-- HEADER -->
    <header>
        <div class="logo">Walled <span>Burger</span></div>
        <nav>
            <ul>
                <li><a href="#home">Home</a></li>
                <li><a href="#menu">Menu</a></li>
                <li><a href="#contact">Contact</a></li>
            </ul>
        </nav>
        <div class="cart-icon" onclick="toggleCart()">
            <i class="fas fa-shopping-cart fa-lg"></i>
            <span class="cart-count" id="cartCount">0</span>
        </div>
    </header>

    <!-- HERO -->
    <section class="hero" id="home">
        <h1>Taste the Fortitude</h1>
        <p style="font-size: 1.2rem; margin-bottom: 20px;">Best Burgers & Pizzas in Town</p>
        <a href="#menu" class="btn">Order Now</a>
    </section>

    <!-- MENU -->
    <section class="menu-section" id="menu">
        <h2 class="section-title">Our Menu</h2>
        
        <div class="category-tabs">
            <button class="tab-btn active" onclick="filterFood('all')">All</button>
            <button class="tab-btn" onclick="filterFood('burger')">Burgers</button>
            <button class="tab-btn" onclick="filterFood('pizza')">Pizzas</button>
        </div>

        <div class="menu-grid" id="menuGrid">
            <!-- Food items inserted by JS -->
        </div>
    </section>

    <!-- INFO & CONTACT -->
    <section class="info-section" id="contact">
        <div class="info-box">
            <h3>Contact Us</h3>
            <p><i class="fas fa-phone"></i> +92 303 7869752</p>
            <p><i class="fas fa-envelope"></i> order@walledburger.com</p>
            <p><i class="fas fa-map-marker-alt"></i> Fateh Garh Tajpura, Lahore, Pakistan</p>
            <a href="https://www.google.com/maps/search/?api=1&query=HC84+Q8+Lahore" target="_blank" class="btn" style="margin-top:10px; font-size:0.9rem;">View on Map</a>
        </div>
        <div class="info-box">
            <h3>Opening Hours</h3>
            <p>Mon - Sun: Open 24 Hours</p>
            <p>Close: 3:00 AM</p>
            <p style="color: var(--gold); font-style:
